import './vars.scss';

const white = '#fff';
const dark = '#333';

export const antTheme = {
  components: {
    Menu: {
      colorTextBase: white,
      colorBgBase: dark,
      algorithm: true,
    },
  },
};
